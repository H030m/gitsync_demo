{"file": ".trellis/spec/backend/testing-guidelines.md", "reason": "jest + ts-jest boundary-mock for a Firestore trigger: invoke the raw handler with synthetic before/after, mock Firestore queries + assignTaskFlow + getMessaging().send. Cases: status->done fires, non-transition no-op (no recursion), ready-filter (all deps done vs not), skip-when-assigned but still notify, FCM no-token skip, best-effort single-B failure."}
{"file": ".trellis/spec/backend/database-guidelines.md", "reason": "Verify Rule E (onDocumentUpdated correct here), transition guard prevents recursion when assignTaskFlow writes downstream assigneeId, markIdempotent first, and NO double-counting of counters (markTaskDone + applyAssignment own them, trigger must not increment)."}
{"file": ".trellis/spec/backend/error-handling.md", "reason": "Verify best-effort: each downstream B wrapped so one failure (assignTaskFlow throw / no fcmToken) is logged and the loop continues; the trigger never throws to avoid at-least-once retry storms."}
{"file": ".trellis/spec/backend/quality-guidelines.md", "reason": "Verify v2-only, region, secrets:[openaiKey] declared on onTaskUpdated, raised timeout, no forbidden patterns."}
