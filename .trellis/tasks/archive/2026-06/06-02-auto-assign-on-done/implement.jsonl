{"file": ".trellis/spec/backend/directory-structure.md", "reason": "Trigger body lives in triggers/onTaskUpdated.ts; any new FCM send helper goes in tools/ (e.g. tools/notify.ts), reusing assignTaskFlow from flows/ — keep the layer split."}
{"file": ".trellis/spec/backend/database-guidelines.md", "reason": "Rule C (markIdempotent first — already present), Rule E (this IS onDocumentUpdated and correct because tasks are created todo then updated to done — contrast with onPRMerged), Rule A/B (do NOT re-touch counters: markTaskDone owns the completed task's counters, applyAssignment owns downstream assignment counters — avoid double counting), dependsOn array-contains query is auto-indexed."}
{"file": ".trellis/spec/backend/error-handling.md", "reason": "Best-effort pattern (Q4): a single downstream B failing (OpenAI / no-member HttpsError from assignTaskFlow / no fcmToken) must be caught + logged, not abort the whole trigger; the transition guard + early returns."}
{"file": ".trellis/spec/backend/quality-guidelines.md", "reason": "v2-only, region asia-east1, forbidden patterns; onTaskUpdated must declare secrets:[openaiKey] (it now calls assignTaskFlow which uses OpenAI) and a raised timeoutSeconds."}
{"file": ".trellis/spec/backend/logging-guidelines.md", "reason": "Structured logger for the status transition, downstream found/ready/skipped, assignment + FCM outcomes (best-effort failures logged here)."}
{"file": ".trellis/spec/guides/code-reuse-thinking-guide.md", "reason": "Reuse assignTaskFlow (flows/assignTask.ts) for downstream assignment instead of re-implementing; mirror getMessaging().send pattern from handlers/subscribeToTopic.ts; reuse the existing markIdempotent in the stub. Do not duplicate counter logic."}
