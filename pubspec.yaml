name: campus_events
description: "校園活動報名系統 — 瀏覽校園活動、註冊登入、報名/取消、查看我的報名。"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # state management + routing（課程指定）
  provider: ^6.1.2
  go_router: ^14.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
