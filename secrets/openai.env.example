# 複製此檔為 openai.env 並填入真實值
# Cloud Functions 本機 emulator 從 functions/.secret.local 讀；正式部署從 Firebase Secret Manager 讀。
# 這個檔案是「人類記住 key 是什麼」的中央備份。

OPENAI_API_KEY=sk-proj-REPLACE_ME

# 若要為不同 model 用不同 organization / project key 可加：
# OPENAI_ORG_ID=org-REPLACE_ME
# OPENAI_PROJECT_ID=proj_REPLACE_ME
