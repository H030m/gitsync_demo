# 許廷煥 (113062340) 工作日誌

> 最新日期在**最上面**。每次寫程式完都要在這裡寫一則。格式參考 [`AI_AGENT_RULES.md §2.2`](../AI_AGENT_RULES.md#22-每篇日誌必填項目)。
>
> 寫完一則後**記得**也更新 [`_index.md`](./_index.md)。

---

## 2026-06-13 — 任務狀態編輯雙入口（06-13-task-status-editor，續上一個 task）

**做了什麼**
- 使用者驗收收合清單後發現缺口：手機上唯一的狀態轉移只剩圓圈「→完成」，而且
  **詳情頁的狀態 chip 一直是唯讀的**（先前被寬螢幕拖拉掩蓋）。
- 新增共用 `showStatusPicker` bottom sheet（`views/tasks/widgets/status_picker.dart`，
  per-status 色票、當前狀態打勾、選同狀態為 no-op），兩個入口：
  (1) 詳情頁主任務 chip 變可點（含下拉箭頭 affordance）；(2) 清單列長按。
- 相關任務列表的 chip 維持唯讀；圓圈完成/點列導頁/寬螢幕拖拉皆不變。
- 新 l10n 字串 `changeStatusTitle`；新測試 4 個（含新建 `test/task_details_test.dart` harness）。

**為何這樣做**
- 詳情頁是「看到狀態→點它→改它」的自然位置；長按清單列補手機快速操作。
- 同一個 picker 共用，避免兩份 sheet code。

**做不下去的地方 / 給下一個人**
- 詳情頁 chip 顯示的還是 wire 字串（todo/in_progress/done）——既有行為，沒順手改，
  想在地化要連 `_StatusChip` 一起調。

**動了哪些檔案**
- `lib/views/tasks/widgets/status_picker.dart`（new）
- `lib/views/tasks/task_details_page.dart`、`lib/views/tasks/tasks_board_page.dart`
- `lib/l10n/app_strings.dart`（+1 字串）
- `test/task_details_test.dart`（new）、`test/tasks_board_test.dart`

**驗證了什麼 / 還沒驗證**
- ✅ analyze 0/0；`flutter test` **85/85**
- ✅ 模擬器 fake + live 都親測（亮暗模式 OK）

---

## 2026-06-13 — Tasks 看板手機版重設計：收合式三段清單（06-13-mobile-board-sections）

**做了什麼**
- 手機寬度的 Tasks 看板從「三條 200dp 欄位橫向捲動」改成 TickTick 風格垂直收合清單：
  待辦/進行中/完成三個 section header（沿用 `_ColumnTheme` 色票 + `_CountChip`），
  點擊展開/收合（`AnimatedSize` + chevron 旋轉），預設 待辦+進行中 開、完成 收。
- 任務列 = 圓圈 + 標題 + 負責人頭像；點列進詳情頁；**點圓圈直接標記完成**
  （錯誤跳 SnackBar；完成區的圓圈為實心且不可點）。
- 寬螢幕三欄 kanban + 拖拉完全不動（responsive 分支只換窄的那邊）。
- 測試重寫：移除 temmie 06-12 簡化卡片遺留的 2 個紅測試（測已刪除的 UI），
  新增 5 個清單行為測試（header/預設展開/收合 toggle/圓圈完成/點列導頁）。

**為何這樣做**
- 橫向捲動窄欄在手機上體驗差（使用者實際抱怨）；demo 主場景是手機。
- 「勾圓圈=完成」直達 demo 核心鏈路：勾完成 → AI 自動分派下游 → 隊友收 FCM 推播。

**做不下去的地方 / 給下一個人**
- 收合動畫小瑕疵：收合時內容先消失、框再縮（cosmetic，AnimatedSize 特性），有空可換
  AnimatedCrossFade/SizeTransition 細修。
- 空 section 的「—」與錯誤 SnackBar 路徑沒有 widget test（非 DoD 必要項）。

**動了哪些檔案**
- `lib/views/tasks/tasks_board_page.dart`（窄分支重寫，+3 widgets）
- `test/tasks_board_test.dart`（-2 紅測試、+5 新測試、+_StubNav）
- `.trellis/spec/frontend/component-guidelines.md`（手機看板新慣例）

**驗證了什麼 / 還沒驗證**
- ✅ `flutter analyze` 0 error/0 warning；`flutter test` **81/81**（整套全綠，測試債清零）
- ✅ 模擬器 fake mode 親測：展開收合/點列/勾完成正常
- ⚠️ 暗色模式：色票全走 ColorScheme 理論安全，模擬器親測補一下更穩

---

## 2026-06-12 — FCM live 端到端驗證全過 + PR #38 併入 main，task 收工

**做了什麼**
- 環境補齊：`flutterfire configure --platforms=android`（真實 google-services.json）、
  Firebase Console 登記本機 debug SHA-1。
- Live e2e 全數驗證通過（Android emulator、live mode、GitHub 登入）：
  - `users/{uid}.fcmToken` 登入後成功寫入 Firestore ✓
  - 看板標 done → `onTaskUpdated` 自動通知下游 assignee：前景重畫可見通知 ✓
    （log `[FCM foreground] 有新任務可以開始了`，per-locale 繁中文案同步驗證）、
    背景系統推播 ✓、點擊導頁 ✓
  - 權限拒絕 → SnackBar 提示 ✓（06-10 修補的行為）
- 併入最新 develop（意外零衝突）→ analyze 0 warn、test 79/79 → PR #38 →
  **經隊友同意直接併入 main**。

**為何這樣做**
- 直接合 main 偏離 git-workflow 慣例，是隊友當下同意的決定（demo 在即）。

**做不下去的地方 / 給下一個人**
- ⚠️ **develop 尚未包含 FCM 工作**，main/develop 分岔（9 vs 7 commits）——請 owner 把
  main back-merge 回 develop，否則 develop 上的開發拿不到通知功能。
- 後端既有 bug（與本 task 無關）：Daily 頁 `summarizeDay` callable 回
  `[firebase_functions/internal]`——需要 owner 看 cloud log（懷疑 Cloud Run 公開存取
  或函式內部錯，參考 SETUP §5.9(c)）。
- 測試殘留：團隊 repo 看板上的 test-A/test-B/test-A2/test-B2 已請本人刪除（若還在請清）。
- 模擬器 DNS 兩度抽風造成假錯誤（Firestore 斷流、App 內報錯）——冷重啟
  `emulator -no-snapshot-load -dns-server 8.8.8.8` 可解，已記入 prd 備註。

**動了哪些檔案**
- 本次無 code 變更（純驗證 + merge）；`.trellis/tasks/06-03-*/prd.md`（驗收紀錄）、journal。

**驗證了什麼 / 還沒驗證**
- ✅ FCM 端到端（前景/背景/點擊/在地化）— 本 task 的核心驗收全綠
- ✅ merge 後 analyze + test 79/79
- ❌ Web push（service worker/VAPID）— out of scope，demo 走手機

---

## 2026-06-10 — 接手 FCM 通知 task：修權限被拒的靜默失敗

**做了什麼**
- 接手 `06-03-wire-fcm-notifications`（原 assignee 嘉駿/opal，經同意轉移；分支
  `feature/foreground-notifications`，他的 4 個 commits 保留續作）。
- 在 Android emulator（fake mode、cold install）重現嘉駿回報的「測試通知按了沒彈」：
  **乾淨環境重現不出來**——code 本身是好的；但實測證實「通知權限被拒後按按鈕完全無回饋」。
- 修補靜默失敗：`local_notifications.dart` 加 `areNotificationsEnabled()` +
  `ensurePermission()`（被拒時 re-prompt 一次）；Settings 測試按鈕先確權限，仍被拒則
  SnackBar 提示（l10n `notificationsDisabledHint`，en+zh）。模擬器實測兩個路徑都正確。

**為何這樣做**
- 不重寫：前景通知實作是標準做法且量小，問題出在「失敗不可見」而非設計。
- FCM `onMessage` 的前景重畫**刻意不**做權限 re-prompt（被動監聽不該彈權限框）。

**做不下去的地方 / 給下一個人**
- live 端到端（後端 `notifyAssignee` → 手機推播 → 點擊導頁）仍未驗證——我的機器還沒
  跑 `flutterfire configure` / 登記 debug SHA，補完環境後測。
- 分支併回 develop 時 `settings_page.dart` 與 UI redesign **預期衝突**，要手動解。

**動了哪些檔案**
- `lib/services/local_notifications.dart`（+2 methods）
- `lib/views/settings/settings_page.dart`（onTap 權限檢查）
- `lib/l10n/app_strings.dart`（+1 字串）
- `.trellis/spec/frontend/{component,quality}-guidelines.md`、`docs/SETUP.md §5.10`（lesson 沉澱）
- `.trellis/tasks/06-03-wire-fcm-notifications/`（prd 更新、assignee 轉移、jsonl 填寫）

**驗證了什麼 / 還沒驗證**
- ✅ `flutter analyze` 0 error/0 warning（僅既有 info）
- ✅ `flutter test` 79/79
- ✅ Android emulator 實測：權限開→彈通知；權限關→SnackBar 提示
- ❌ live FCM 端到端（環境未備）
- ❌ 與最新 develop 的合併

---

## 2026-05-26 — Journal 初始化

**做了什麼**
- 建立個人日誌檔。

**為何這樣做**
- 跟 AI_AGENT_RULES.md 規範一致。

**做不下去的地方 / 給下一個人**
- 還沒開始寫 code，待分配模組。

**動了哪些檔案**
- `docs/journal/113062340_tinghuan.md` (new)

**驗證了什麼 / 還沒驗證**
- N/A
